services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xcellimark-scraper
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
